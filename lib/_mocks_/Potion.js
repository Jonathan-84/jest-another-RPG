module.exports = function() {
    this.name = 'health';
    this.value = 20;
  };

  // look into, the pciture looks different then my results.
  // Mine says potion {}, theirs shows {name: 'health, value: 20}